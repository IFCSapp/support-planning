# 支援計画ナビ 標準辞書の更新手順

この文書は、支援計画ナビの標準辞書を更新するときの手順をまとめたものです。

対象ファイルは次のファイルです。

```text
public/data/support_plan_dictionary_v0_2_1.json
```

ファイル名は `v0_2_1` のままですが、辞書内部の `schemaVersion` を標準辞書のバージョンとして扱います。

---

## 1. 更新前に確認すること

作業前に、現在の状態でビルドできるか確認します。

```powershell
npm run build
```

ここでエラーが出る場合は、辞書更新の前にエラーを直します。

---

## 2. 標準辞書ファイルを開く

VS Codeで次のファイルを開きます。

```text
public/data/support_plan_dictionary_v0_2_1.json
```

先頭付近にある次の項目を確認します。

```json
{
  "schemaVersion": "0.3.0",
  "generatedAt": "2026-04-30",
  "description": "..."
}
```

---

## 3. schemaVersionを上げる

辞書を更新したら、必ず `schemaVersion` を上げます。

表現の微修正なら、末尾を1つ上げます。

```json
"schemaVersion": "0.3.1"
```

大きな領域追加や構造変更をした場合は、次のように上げます。

```json
"schemaVersion": "0.4.0"
```

目安は次のとおりです。

```text
0.3.0 → 0.3.1
  表現修正、誤字修正、語尾修正、少量の語彙追加

0.3.0 → 0.4.0
  支援領域の追加、大量の語彙追加、候補ルールの大幅追加

0.3.0 → 1.0.0
  辞書構造が安定し、標準版として運用できる段階
```

---

## 4. descriptionを更新する

`description` には、今回の更新内容を短く書きます。

例です。

```json
"description": "面談中に語彙を選び、本人向けの3文提案型個別支援計画を生成するための実装用辞書です。v0.3.1では、本人向け提案文と職員支援文の表現を見直しています。"
```

書く内容は、次の程度で十分です。

```text
何を直したか
どの領域を増やしたか
表現修正なのか、語彙追加なのか
```

---

## 5. 表現を修正するときの基本ルール

辞書の表現は、本人向けの提案文として使いやすい形にします。

避ける表現です。

```text
頑張る
意識する
理解する
克服する
自信をつける
ちゃんとする
必要に応じて支援します
本人は〜する
〜していく
```

望ましい表現です。

```text
一つ選ぶ
一文で伝える
手順表を見る
職員と確認する
短い連絡を入れる
最初の一手を決める
相談する内容をメモする
```

抽象的な目標ではなく、場面で実行できる小さな行動にします。

---

## 6. 行動項目を修正するときの注意

行動項目では、`base` と `teForm` をそろえます。

例です。

```json
{
  "id": "act_consult_001",
  "base": "質問内容を一文でメモする",
  "teForm": "質問内容を一文でメモして",
  "domainId": "consultation",
  "shortGoal": "相談を使う",
  "observable": true,
  "smallStep": true
}
```

`teForm` は必ず入れます。

アプリ側の自動変換に頼ると、日本語が不自然になることがあります。自然な文にしたい場合は、辞書側で `teForm` を明示します。

---

## 7. 職員支援を修正するときの注意

職員支援は、職員が何をするかが分かる文にします。

よい例です。

```json
{
  "id": "sup_consult_001",
  "lead": "質問内容を一緒に一文へ整理し",
  "ending": "相談しやすくなるよう支援します",
  "domainId": "consultation",
  "staffSubject": "職員は"
}
```

避ける例です。

```json
{
  "lead": "本人の状況を見守り",
  "ending": "必要に応じて支援します"
}
```

「見守る」「必要に応じて」は、支援操作が分かりにくいため、なるべく使いません。

---

## 8. 場面を修正するときの注意

場面は、本人が「そのときのことだ」と分かる表現にします。

よい例です。

```json
{
  "id": "sit_consult_001",
  "text": "作業中に分からないことが出たとき",
  "domainId": "consultation",
  "sentenceConnector": "は"
}
```

避ける例です。

```text
困難場面
相談が必要な場面
課題が発生した場合
```

抽象語より、実際の場面を短く書きます。

---

## 9. 方向性を修正するときの注意

方向性は増やしすぎないようにします。

方向性は、本人向けの大きな方針です。

よい例です。

```json
{
  "id": "dir_consult_001",
  "text": "分からないことを小さく相談する",
  "sentence": "分からないことを小さく相談しましょう。",
  "domainId": "consultation"
}
```

方向性は、各支援領域に8〜12個程度あれば十分です。

---

## 10. JSONの構文を壊さないための注意

JSONでは、次の点に注意します。

```text
文字列は必ず " " で囲む
項目の区切りには , を入れる
最後の項目の後ろには , を入れない
全角のダブルクォーテーションを使わない
コメントを書かない
```

悪い例です。

```json
{
  "id": "act_001",
  "base": "質問する",
}
```

最後に余分なカンマがあります。

よい例です。

```json
{
  "id": "act_001",
  "base": "質問する"
}
```

---

## 11. 辞書チェックを行う

アプリを起動して、辞書編集画面を開きます。

```powershell
npm run dev
```

画面で次を確認します。

```text
辞書バージョンが上がっている
辞書編集画面が開く
支援領域が表示される
方向性・場面・行動・職員支援が表示される
支援候補ルールが表示される
辞書チェックを実行できる
```

辞書チェックでエラーが出た場合は、該当するIDを修正します。

---

## 12. ビルド確認を行う

更新後、必ずビルドします。

```powershell
npm run build
```

ビルドが通れば、辞書ファイルとしては最低限問題ありません。

---

## 13. アプリで標準辞書を反映する

端末内に編集済み辞書が保存されている場合、標準辞書を更新してもすぐには反映されないことがあります。

その場合は、アプリの辞書画面で次の操作を行います。

```text
辞書を確認する
標準辞書を反映する
```

この操作をすると、現在の編集済み辞書を端末内にバックアップしたうえで、新しい標準辞書に切り替えます。

---

## 14. 標準辞書に戻す操作との違い

「標準辞書を反映する」は、更新された標準辞書へ切り替える操作です。

「標準辞書に戻す」は、現在の編集内容を破棄して標準辞書へ戻す操作です。

表現をブラッシュアップしている途中の場合は、先に編集済み辞書を書き出してから操作します。

---

## 15. 更新時の推奨手順

標準辞書を更新するときは、次の順番で作業します。

```text
1. public/data/support_plan_dictionary_v0_2_1.json を開く
2. 表現や語彙を修正する
3. schemaVersion を上げる
4. description を更新する
5. npm run build を実行する
6. npm run dev で画面確認する
7. 辞書編集画面で辞書チェックを実行する
8. 必要なら標準辞書を反映する
9. Gitにコミットする
```

---

## 16. コミットメッセージ例

表現修正の場合です。

```text
Update dictionary expressions to v0.3.1
```

語彙追加の場合です。

```text
Add consultation and work-start vocabulary to dictionary v0.4.0
```

誤字修正の場合です。

```text
Fix dictionary typos in v0.3.1
```

---

## 17. 表現ブラッシュアップ時の確認観点

表現を直すときは、次の観点で確認します。

```text
本人を評価する文になっていないか
欠点や性格の問題として書いていないか
行動が観察できるか
一回で試せる小ささになっているか
職員支援が具体的な操作になっているか
計画文として読み上げても自然か
「頑張る」「意識する」に逃げていないか
場面と行動がつながっているか
```

---

## 18. 更新履歴の書き方

辞書の更新内容は、必要に応じて別ファイルにも残します。

例です。

```text
docs/dictionary-changelog.md
```

書き方です。

```md
## v0.3.1

- 本人向け提案文の語尾を調整しました。
- 職員支援文の「必要に応じて支援します」を具体的な支援操作へ修正しました。
- 行動項目の teForm を追加・修正しました。
```

---

## 19. トラブル対応

### 辞書が読み込まれない

JSONの構文エラーがある可能性があります。

確認することです。

```text
カンマの付け忘れ
余分なカンマ
ダブルクォーテーションの閉じ忘れ
全角記号の混入
```

### 更新した辞書が画面に出ない

端末内に古い編集済み辞書が残っている可能性があります。

辞書画面で「標準辞書を反映する」を使います。

### 画面が真っ白になる

まずビルドを確認します。

```powershell
npm run build
```

エラーが出る場合は、表示されたファイル名と行番号を確認します。

### 日本語が不自然になる

`teForm` や `sentence` が不足している可能性があります。

自然な文にしたい場合は、自動生成に頼らず、辞書側に `sentence` を直接入れます。

---

## 20. 基本方針

標準辞書は、アプリの中核です。

機能を増やすよりも、次の品質を優先します。

```text
本人向けに読める
現場の面談から選びやすい
職員が支援操作を思い浮かべやすい
小さな行動に落ちている
表現が支援計画に転記しやすい
```
